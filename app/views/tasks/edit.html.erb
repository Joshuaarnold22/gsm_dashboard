<h1>Edit Task</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for tasks</p>
    <ul>
      <li>Please keep them clear and to the point.</li>
      <li>Be precise in their directions.</li>
      <li>Follow up with the supervisor so that they understand.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @task do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control', placeholder: "Enter task title" %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter the task info here." %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
