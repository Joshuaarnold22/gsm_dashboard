<div class="container">
  <h3><%= @task.name %></h3>
  <p><%= @task.description %></p>

  <% if policy(Task.new).edit? %>
    <%= link_to "Edit", edit_task_path(@task), class: 'btn btn-success' %>
  <% end %>
</div>
